name: Actions Quick Check
on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: |
          echo "GitHub Actions works"
          uname -a
